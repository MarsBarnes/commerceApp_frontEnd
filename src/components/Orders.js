import React, { useState } from "react";

const Orders = () => {
    const [ordersData, setOrdersData] = useState([]);
  return (
    <div>
      <h1> Orders</h1>
    </div>
  );
};

export default Orders;
